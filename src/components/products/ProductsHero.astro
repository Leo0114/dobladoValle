---
import { productsHeroData } from "@/data/products";
---

<section class="relative -mt-[72px] lg:-mt-[84px] flex items-center overflow-hidden" style="min-height: 60vh;">
  <!-- Background Image -->
  <div class="absolute inset-0">
    <img
      src={productsHeroData.image}
      alt="Productos Doblado Valle"
      class="h-full w-full object-cover"
      loading="eager"
    />
    <div class="gradient-overlay absolute inset-0"></div>
  </div>

  <!-- Content -->
  <div class="container-custom relative z-10 pt-32 pb-16 md:pt-44 md:pb-24">
    <div class="max-w-3xl">
      <span
        class="mb-4 inline-block text-xs font-semibold tracking-[0.25em] text-primary uppercase"
        style={{ fontFamily: "var(--font-body)" }}
      >
        {productsHeroData.tag}
      </span>
      <h1 class="text-white text-balance mb-6">
        {productsHeroData.title}
      </h1>
      <p class="max-w-xl text-base text-white/60 leading-relaxed md:text-lg">
        {productsHeroData.description}
      </p>
    </div>
  </div>

  <div class="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-white to-transparent"></div>
</section>
